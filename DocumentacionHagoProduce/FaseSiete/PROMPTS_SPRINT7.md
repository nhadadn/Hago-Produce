<h1>🧠 PROMPT MAESTRO — GENERADOR DE PROMPTS SPRINT 7</h1><h2>VibeCoding Prompt Architect v1.0</h2><hr><h2>🎭 ROL</h2><p>Eres un <strong>VibeCoding Prompt Architect</strong>.</p><p>Tu única responsabilidad en esta sesión es: <strong>Generar la serie completa de prompts de implementación para el Sprint 7 de Hago Produce, basándote estrictamente en las decisiones del CTO_STRATEGIC_DESIGN_SPRINT7.</strong></p><p>No generas código. No generas documentación. No opinas sobre las decisiones arquitectónicas — ya están tomadas. Solo generas prompts.</p><p>Un prompt que generas tiene estas propiedades:</p><ul> <li><strong>Una sola responsabilidad</strong> — hace exactamente una cosa</li> <li><strong>Contexto suficiente</strong> — el LLM que lo ejecute no necesita preguntar nada</li> <li><strong>Contrato explícito</strong> — define inputs, outputs, y criterio de éxito</li> <li><strong>Restricciones integradas</strong> — incluye lo que NO debe hacer</li> <li><strong>Verificable</strong> — tiene un criterio de done medible</li> </ul><hr><h2>📜 TU CONSTITUCIÓN (LEY SUPREMA)</h2><p>Todo prompt que generes debe respetar estas decisiones del CTO. No las cuestiones. No las modifiques. No las ignores.</p><h3>Stack Decidido</h3><ul> <li>Next.js 14 App Router + TypeScript strict</li> <li>PostgreSQL (Neon) + Prisma ORM</li> <li>OpenAI SDK (structured outputs)</li> <li>WhatsApp Cloud API (Meta directo)</li> <li>Telegram Bot API</li> <li>Resend (email)</li> <li>Vercel + Neon</li> </ul><h3>Lo que NO existe en Sprint 7</h3><ul> <li>❌ Redis — ReportCache en PG ya existe y es suficiente</li> <li>❌ BullMQ / colas distribuidas — conversión síncrona (&lt;100 PreInvoices/semana)</li> <li>❌ CRA XML / firma X.509 — Sprint 8+</li> <li>❌ Stripe — Sprint 8+</li> <li>❌ Multi-tenancy — Sprint 9+</li> <li>❌ Eliminar dual write ProductPrice — Sprint 8</li> </ul><h3>Restricciones No Negociables</h3><ol> <li>No Redis antes de resolver N+1 queries</li> <li>No BullMQ — conversión síncrona para PreInvoice</li> <li>No CRA XML — solo TaxCalculationService + campos nullable</li> <li>No eliminar /webhooks/make/prices — deprecar, mantener funcionando</li> <li>No modificar datos existentes en migraciones — solo agregar nullable</li> <li>No llamadas directas de intents a servicios de dominio — todo por CommandBus</li> <li>No prompts hardcodeados en openai-client.ts — todo por PromptBuilder</li> <li>No migraciones en producción sin staging verde primero</li> <li>No nuevos features sin tests que los cubran</li> <li>No scope creep — si no está en la secuencia del CTO, no va en este sprint</li> </ol><h3>Boundaries de Dominio (Reglas de Llamada)</h3><ul> <li>HTTP Route → valida con Zod → llama a Service → NO llama a otro Service</li> <li>PriceVersioningService → SOLO lee, NO hace CRUD, NO conoce el chat</li> <li>CommandBus → único punto de entrada para acciones transaccionales del chat</li> <li>PdfIngestionService → SOLO extrae texto, NO conoce dominio de precios</li> <li>DocumentMapperService → SOLO mapea campos, NO llama a Prisma directamente</li> <li>TaxCalculationService → SOLO calcula montos, NO genera XML, NO llama a Prisma</li> <li>PromptBuilder → SOLO construye strings, NO llama a OpenAI, NO conoce dominio</li> <li>PreInvoiceConversionService → SOLO convierte, NO aprueba, NO rechaza</li> <li>PreInvoiceService → SOLO aprueba/rechaza, NO convierte</li> </ul><h3>Secuencia de Bloques (Orden Obligatorio)</h3><p>BLOQUE 0 — Prerequisitos B0-01: Fix PriceVersion tests B0-02: Feature flags setup</p><p>BLOQUE 1 — PreInvoice Conversion B1-01: PreInvoiceStatus enum B1-02: PreInvoiceService - approve() B1-03: PreInvoiceService - reject() B1-04: PreInvoiceConversionService B1-05: PreInvoice conversion tests B1-06: API endpoint - approve B1-07: API endpoint - reject B1-08: API endpoint - convert</p><p>BLOQUE 2 — Notificaciones B2-01: NotificationService - PreInvoice events B2-02: Email template - approval request B2-03: Chat notification - approval B2-04: Notification tests</p><p>BLOQUE 3 — ProductPrice Migration B3-01: Audit ProductPrice readers B3-02: Migrate product-prices.service.ts B3-03: Migrate price-lookup.ts intent B3-04: Migrate /api/v1/product-prices/ endpoints B3-05: Migration tests</p><p>BLOQUE 4 — Hardening B4-01: Coverage audit B4-02: Console.* audit B4-03: Integration tests - PreInvoice flow B4-04: Performance tests - conversion</p><p>BLOQUE 5 — Documentación B5-01: Update DECISION_LOG.md B5-02: Update README - PreInvoice flow B5-03: Update GROUND_TRUTH_SPRINT7.md</p><hr><h2>⚙️ REGLAS DE GENERACIÓN DE PROMPTS</h2><h3>Regla 1 — Un Prompt, Una Responsabilidad</h3><p>Cada prompt implementa exactamente un ítem del backlog. No combines B1-02 y B1-03 en un solo prompt. No dividas B1-04 en 5 prompts micro. El granulado correcto es: un bloque del CTO = un prompt.</p><h3>Regla 2 — El Prompt Debe Ser Autocontenido</h3><p>El LLM que ejecute el prompt no debe necesitar:</p><ul> <li>Leer el CTO_STRATEGIC_DESIGN</li> <li>Preguntar qué tecnología usar</li> <li>Adivinar el nombre de archivos existentes</li> <li>Inferir qué restricciones aplican</li> </ul><p>Todo eso va dentro del prompt.</p><h3>Regla 3 — Contexto Antes de Instrucción</h3><p>Cada prompt sigue esta estructura interna:</p><p>[CONTEXTO] — Qué existe hoy en el repo relevante para esta tarea [TAREA] — Qué debe construirse, con precisión quirúrgica [RESTRICCIONES] — Qué NO debe hacer el LLM al ejecutar este prompt [CONTRATO] — Inputs, outputs, archivos a crear/modificar [CRITERIO DE DONE] — Cómo saber que el prompt fue ejecutado correctamente</p><h3>Regla 4 — Nombra lo que Existe</h3><p>Cada prompt debe mencionar explícitamente:</p><ul> <li>Archivos existentes que el LLM debe leer antes de generar</li> <li>Servicios existentes que el LLM debe respetar</li> <li>Modelos de Prisma existentes que el LLM debe extender (no reemplazar)</li> <li>Variables de entorno que ya existen vs. las nuevas que se necesitan</li> </ul><h3>Regla 5 — Restricciones Integradas, No Separadas</h3><p>Las restricciones no van al final como nota. Van dentro de la instrucción principal. Ejemplo incorrecto: "Crea el servicio. Nota: no uses Redis." Ejemplo correcto: "Crea el servicio usando PostgreSQL directamente (no Redis — ReportCache en PG ya existe y es suficiente para este volumen)."</p><h3>Regla 6 — El Criterio de Done es Medible</h3><p>No uses: "el servicio debe funcionar correctamente" Usa: "el servicio pasa los siguientes casos de test: [lista]" "el endpoint retorna 201 con el schema [X] cuando..." "la migración se aplica sin errores y los datos existentes están intactos"</p><h3>Regla 7 — Dependencias Explícitas</h3><p>Si el prompt B1-02 depende de B1-01, el prompt de B1-02 debe decir: "Este prompt asume que B1-01 ya fue ejecutado y que PreInvoiceStatus enum existe en prisma/schema.prisma"</p><h3>Regla 8 — Código de Schema Incluido</h3><p>Si el prompt requiere cambios en el schema de Prisma, incluye el código Prisma exacto dentro del prompt. No digas "agrega los campos necesarios". Di exactamente cuáles.</p><h3>Regla 9 — Sin Ambigüedad en Naming</h3><p>Cada prompt especifica nombres exactos:</p><ul> <li>Nombre del archivo: <code>pre-invoice-conversion.service.ts</code></li> <li>Nombre de la clase: <code>PreInvoiceConversionService</code></li> <li>Nombre del método: <code>convertToInvoice(preInvoiceId: string): Promise&lt;Invoice&gt;</code></li> <li>Nombre de la ruta: <code>POST /api/v1/pre-invoices/:id/convert</code></li> </ul><h3>Regla 10 — Prompt de Verificación al Final</h3><p>Después de generar todos los prompts de implementación, genera un prompt final de verificación que valide que el sprint completo está integrado y funcionando.</p><hr><h2>📋 FORMATO DE CADA PROMPT GENERADO</h2><p>Cada prompt que generes debe seguir este formato exacto:</p><hr><p>[S7-BX-XX] [Nombre del Ítem] Bloque: [Nombre del Bloque] | Dependencias: [lista de códigos previos]</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>[Describe qué existe hoy en el repo que es relevante para esta tarea. Nombra archivos, servicios, modelos de Prisma, endpoints existentes. Sé específico. El LLM que ejecute esto no tiene acceso al repo.]</p><h3>TAREA</h3><p>[Instrucción principal. Una sola responsabilidad. Usa verbos de acción: Crea, Extrae, Refactoriza, Agrega, Migra. Incluye las restricciones dentro de la instrucción, no separadas.]</p><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>[Lista exacta de archivos con su ruta completa]</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong> [Tipos TypeScript exactos del input]</p><p><strong>Output:</strong> [Tipos TypeScript exactos del output]</p><p><strong>Variables de entorno nuevas (si aplica):</strong> [Nombre y descripción]</p><p><strong>Schema Prisma (si aplica):</strong> [Código Prisma exacto]</p><h3>RESTRICCIONES</h3><p>[Lista de lo que el LLM NO debe hacer al ejecutar este prompt]</p><h3>CRITERIO DE DONE</h3><ul> <li>[Criterio medible 1]</li> <li>[Criterio medible 2]</li> <li>[Criterio medible N]</li> </ul><hr><hr><h2>🚀 INSTRUCCIÓN DE EJECUCIÓN</h2><p>Ahora genera la serie completa de prompts para el Sprint 7 de Hago Produce.</p><p>Genera un prompt por cada ítem del backlog, en el orden exacto de la secuencia del CTO:</p><p>B0-01 → B0-02 → B1-01 → B1-02 → B1-03 → B1-04 → B1-05 → B1-06 → B1-07 → B1-08 → B2-01 → B2-02 → B2-03 → B2-04 → B3-01 → B3-02 → B3-03 → B3-04 → B3-05 → B4-01 → B4-02 → B4-03 → B4-04 → B5-01 → B5-02 → B5-03 → PROMPT DE VERIFICACIÓN FINAL</p><p>Para cada prompt:</p><ol> <li>Lee la sección correspondiente del CTO_STRATEGIC_DESIGN</li> <li>Extrae el contexto técnico exacto (archivos, modelos, servicios)</li> <li>Formula la tarea con una sola responsabilidad</li> <li>Integra las restricciones dentro de la instrucción</li> <li>Define el contrato técnico con tipos exactos</li> <li>Establece el criterio de done medible</li> </ol><hr><h2>📝 PROMPTS GENERADOS</h2><hr><p>[S7-B0-01] Fix PriceVersion tests Bloque: BLOQUE 0 - PREREQUISITOS | Dependencias: Ninguna</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El repositorio tiene tests de PriceVersion que están fallando porque el Prisma Client no ha sido regenerado después de cambios en el schema. Los tests están en <code>src/lib/services/pricing/__tests__/price-versioning.service.test.ts</code>. El servicio PriceVersioningService existe en <code>src/lib/services/pricing/price-versioning.service.ts</code> y tiene 100% coverage según el GROUND_TRUTH. El schema de Prisma tiene el modelo PriceVersion definido con las relaciones correctas a PriceList y Product.</p><h3>TAREA</h3><p>Ejecuta <code>npx prisma generate</code> para regenerar el Prisma Client con los cambios más recientes del schema, luego ejecuta los tests de PriceVersion para verificar que pasan. Si hay tests fallando por otros motivos (no por Prisma Client), arregla los tests para que pasen manteniendo la lógica del servicio intacta. NO modifiques el servicio PriceVersioningService, solo los tests si es necesario.</p><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>MODIFICAR: src/lib/services/pricing/<strong>tests</strong>/price-versioning.service.test.ts (si es necesario)</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong> Ninguno (comando CLI)</p><p><strong>Output:</strong> Prisma Client regenerado y tests de PriceVersion pasando</p><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno (solo regeneración)</p><h3>RESTRICCIONES</h3><ul> <li>NO modificar el servicio PriceVersioningService</li> <li>NO cambiar la lógica de negocio del servicio</li> <li>NO agregar nuevos tests, solo arreglar los existentes</li> <li>NO usar Redis o BullMQ</li> <li>NO modificar el schema de Prisma</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li><code>npx prisma generate</code> se ejecuta sin errores</li> <li>Todos los tests en <code>src/lib/services/pricing/__tests__/price-versioning.service.test.ts</code> pasan</li> <li>El servicio PriceVersioningService mantiene 100% coverage</li> <li>No hay cambios en la lógica de negocio del servicio</li> </ul><hr><p>[S7-B0-02] Feature flags setup Bloque: BLOQUE 0 - PREREQUISITOS | Dependencias: Ninguna</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El repositorio no tiene un sistema de feature flags implementado. Para Sprint 7 necesitamos feature flags para migrar gradualmente los lectores de ProductPrice a PriceVersion. El proyecto usa Next.js 14 con TypeScript. Variables de entorno se manejan en <code>.env.example</code> y <code>.env.staging.example</code>. No hay dependencias de LaunchDarkly ni servicios similares.</p><h3>TAREA</h3><p>Crea un sistema simple de feature flags usando variables de entorno y un helper en TypeScript. El sistema debe permitir habilitar/deshabilitar features individualmente en staging y producción. Crea el archivo <code>src/lib/feature-flags.ts</code> con un helper <code>isFeatureEnabled(featureName: string): boolean</code> que lea de variables de entorno con el formato <code>FEATURE_&lt;FEATURE_NAME&gt;=true|false</code>. Agrega las variables de entorno necesarias en <code>.env.example</code> y <code>.env.staging.example</code> para las features de Sprint 7: <code>USE_PRICE_VERSION_FOR_PRICING_SERVICE</code>, <code>USE_PRICE_VERSION_FOR_PRICE_LOOKUP</code>, <code>USE_PRICE_VERSION_FOR_API_ENDPOINTS</code>.</p><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/lib/feature-flags.ts MODIFICAR: .env.example MODIFICAR: .env.staging.example</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">featureName: string // Nombre de la feature (ej: "USE_PRICE_VERSION_FOR_PRICING_SERVICE")
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">boolean // true si la feature está habilitada, false si no
</code></pre><p><strong>Variables de entorno nuevas:</strong></p><ul> <li><code>FEATURE_USE_PRICE_VERSION_FOR_PRICING_SERVICE=true|false</code></li> <li><code>FEATURE_USE_PRICE_VERSION_FOR_PRICE_LOOKUP=true|false</code></li> <li><code>FEATURE_USE_PRICE_VERSION_FOR_API_ENDPOINTS=true|false</code></li> </ul><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO usar servicios externos como LaunchDarkly</li> <li>NO crear una UI para gestionar feature flags</li> <li>NO usar Redis para almacenar feature flags</li> <li>Solo usar variables de entorno</li> <li>El helper debe ser type-safe con TypeScript</li> <li>Las features deben estar deshabilitadas por defecto (false)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li><code>src/lib/feature-flags.ts</code> existe con el helper <code>isFeatureEnabled()</code></li> <li><code>.env.example</code> tiene las 3 variables de entorno con valor <code>false</code></li> <li><code>.env.staging.example</code> tiene las 3 variables de entorno con valor <code>false</code></li> <li>El helper retorna <code>false</code> cuando la variable no está definida</li> <li>El helper es type-safe con TypeScript</li> <li>Tests unitarios para el helper (opcional pero recomendado)</li> </ul><hr><p>[S7-B1-01] PreInvoiceStatus enum Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B0-01</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El schema de Prisma tiene el enum <code>PreInvoiceStatus</code> definido actualmente con los valores: <code>DRAFT</code>, <code>PENDING_APPROVAL</code>. El modelo <code>PreInvoice</code> existe en <code>prisma/schema.prisma</code> con el campo <code>status PreInvoiceStatus @default(DRAFT)</code>. Para Sprint 7 necesitamos agregar los estados <code>APPROVED</code>, <code>REJECTED</code>, <code>CONVERTED</code> para implementar el flujo de aprobación y conversión.</p><h3>TAREA</h3><p>Agrega los valores <code>APPROVED</code>, <code>REJECTED</code>, <code>CONVERTED</code> al enum <code>PreInvoiceStatus</code> en <code>prisma/schema.prisma</code>. Ejecuta <code>npx prisma migrate dev --name add_pre_invoice_status_values</code> para crear la migración. NO modifiques otros campos del modelo PreInvoice. NO elimines los valores existentes del enum.</p><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>MODIFICAR: prisma/schema.prisma CREAR: prisma/migrations/[timestamp]_add_pre_invoice_status_values/migration.sql (generado por Prisma)</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong> Ninguno (modificación de schema)</p><p><strong>Output:</strong> Enum PreInvoiceStatus actualizado con 5 valores: DRAFT, PENDING_APPROVAL, APPROVED, REJECTED, CONVERTED</p><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong></p><pre><code class="language-prisma">enum PreInvoiceStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  REJECTED
  CONVERTED
}
</code></pre><h3>RESTRICCIONES</h3><ul> <li>NO eliminar los valores existentes DRAFT y PENDING_APPROVAL</li> <li>NO modificar otros campos del modelo PreInvoice</li> <li>NO crear nuevos enums</li> <li>NO modificar datos existentes en la migración (solo ALTER TYPE)</li> <li>La migración debe ser reversible (down migration)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El enum PreInvoiceStatus tiene 5 valores: DRAFT, PENDING_APPROVAL, APPROVED, REJECTED, CONVERTED</li> <li>La migración se aplica sin errores en desarrollo</li> <li>La migración es reversible (<code>prisma migrate resolve --rolled-back [migration_name]</code>)</li> <li>No hay errores de validación en Prisma Studio</li> <li>Los tests existentes de PreInvoice siguen pasando</li> </ul><hr><p>[S7-B1-02] PreInvoiceService - approve() Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-01</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio PreInvoiceService existe en <code>src/lib/services/pre-invoice.service.ts</code> (o debe crearse si no existe). El modelo PreInvoice tiene el campo <code>status PreInvoiceStatus</code>. Para Sprint 7 necesitamos implementar el método <code>approve()</code> que cambie el estado de <code>PENDING_APPROVAL</code> a <code>APPROVED</code>. El servicio debe usar LoggerService para logging y Prisma Client para operaciones de base de datos. El servicio debe validar que el PreInvoice esté en estado <code>PENDING_APPROVAL</code> antes de aprobarlo.</p><h3>TAREA</h3><p>Implementa el método <code>approve(preInvoiceId: string, approvedBy: string): Promise&lt;PreInvoice&gt;</code> en PreInvoiceService. El método debe:</p><ol> <li>Validar que el PreInvoice existe</li> <li>Validar que el estado actual es <code>PENDING_APPROVAL</code></li> <li>Cambiar el estado a <code>APPROVED</code></li> <li>Guardar el <code>approvedBy</code> (userId) en un campo nuevo <code>approvedById</code> (agregar al schema si no existe)</li> <li>Actualizar <code>approvedAt</code> con la fecha actual (agregar al schema si no existe)</li> <li>Retornar el PreInvoice actualizado</li> <li>Lanzar error si el PreInvoice no está en estado <code>PENDING_APPROVAL</code></li> <li>Usar LoggerService para logging de la aprobación</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>MODIFICAR: prisma/schema.prisma (agregar campos approvedById, approvedAt a PreInvoice) CREAR: prisma/migrations/[timestamp]_add_pre_invoice_approval_fields/migration.sql MODIFICAR: src/lib/services/pre-invoice.service.ts (o CREAR si no existe) CREAR: src/lib/services/pre-invoice/<strong>tests</strong>/approve.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">preInvoiceId: string // UUID del PreInvoice
approvedBy: string // userId del usuario que aprueba
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">Promise&lt;PreInvoice&gt; // PreInvoice actualizado con status=APPROVED
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong></p><pre><code class="language-prisma">model PreInvoice {
  // ... campos existentes
  status PreInvoiceStatus @default(DRAFT)
  approvedById String? @map("approved_by_id")
  approvedBy User? @relation("PreInvoiceApprovals", fields: [approvedById], references: [id])
  approvedAt DateTime? @map("approved_at")
  // ... campos existentes
}
</code></pre><h3>RESTRICCIONES</h3><ul> <li>NO aprobar PreInvoices que no estén en estado <code>PENDING_APPROVAL</code></li> <li>NO permitir aprobar PreInvoices ya aprobados</li> <li>NO usar Redis o BullMQ</li> <li>NO llamar a otros servicios desde PreInvoiceService</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Usar Prisma.Decimal para montos monetarios</li> <li>La operación debe ser atómica (transacción de Prisma)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El método <code>approve()</code> existe en PreInvoiceService</li> <li>El método cambia el estado de <code>PENDING_APPROVAL</code> a <code>APPROVED</code></li> <li>El método guarda <code>approvedById</code> y <code>approvedAt</code></li> <li>El método lanza error si el estado no es <code>PENDING_APPROVAL</code></li> <li>Tests unitarios cubren: aprobación exitosa, error por estado incorrecto, error por PreInvoice no encontrado</li> <li>Coverage del método ≥90%</li> <li>LoggerService se usa para logging (no console.*)</li> </ul><hr><p>[S7-B1-03] PreInvoiceService - reject() Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-01</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio PreInvoiceService existe en <code>src/lib/services/pre-invoice.service.ts</code> (creado en S7-B1-02). El modelo PreInvoice tiene el campo <code>status PreInvoiceStatus</code>. Para Sprint 7 necesitamos implementar el método <code>reject()</code> que cambie el estado de <code>PENDING_APPROVAL</code> a <code>REJECTED</code> con un motivo de rechazo. El servicio debe usar LoggerService para logging y Prisma Client para operaciones de base de datos.</p><h3>TAREA</h3><p>Implementa el método <code>reject(preInvoiceId: string, rejectedBy: string, reason: string): Promise&lt;PreInvoice&gt;</code> en PreInvoiceService. El método debe:</p><ol> <li>Validar que el PreInvoice existe</li> <li>Validar que el estado actual es <code>PENDING_APPROVAL</code></li> <li>Cambiar el estado a <code>REJECTED</code></li> <li>Guardar el <code>rejectedBy</code> (userId) en un campo nuevo <code>rejectedById</code> (agregar al schema si no existe)</li> <li>Guardar el <code>reason</code> en un campo nuevo <code>rejectionReason</code> (agregar al schema si no existe)</li> <li>Actualizar <code>rejectedAt</code> con la fecha actual (agregar al schema si no existe)</li> <li>Retornar el PreInvoice actualizado</li> <li>Lanzar error si el PreInvoice no está en estado <code>PENDING_APPROVAL</code></li> <li>Validar que <code>reason</code> no esté vacío y tenga máximo 500 caracteres</li> <li>Usar LoggerService para logging del rechazo</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>MODIFICAR: prisma/schema.prisma (agregar campos rejectedById, rejectionReason, rejectedAt a PreInvoice) CREAR: prisma/migrations/[timestamp]_add_pre_invoice_rejection_fields/migration.sql MODIFICAR: src/lib/services/pre-invoice.service.ts CREAR: src/lib/services/pre-invoice/<strong>tests</strong>/reject.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">preInvoiceId: string // UUID del PreInvoice
rejectedBy: string // userId del usuario que rechaza
reason: string // Motivo del rechazo (1-500 caracteres)
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">Promise&lt;PreInvoice&gt; // PreInvoice actualizado con status=REJECTED
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong></p><pre><code class="language-prisma">model PreInvoice {
  // ... campos existentes
  status PreInvoiceStatus @default(DRAFT)
  rejectedById String? @map("rejected_by_id")
  rejectedBy User? @relation("PreInvoiceRejections", fields: [rejectedById], references: [id])
  rejectionReason String? @map("rejection_reason") @db.VarChar(500)
  rejectedAt DateTime? @map("rejected_at")
  // ... campos existentes
}
</code></pre><h3>RESTRICCIONES</h3><ul> <li>NO rechazar PreInvoices que no estén en estado <code>PENDING_APPROVAL</code></li> <li>NO permitir rechazar PreInvoices ya rechazados o aprobados</li> <li>NO usar Redis o BullMQ</li> <li>NO llamar a otros servicios desde PreInvoiceService</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Validar que <code>reason</code> no esté vacío y tenga máximo 500 caracteres</li> <li>La operación debe ser atómica (transacción de Prisma)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El método <code>reject()</code> existe en PreInvoiceService</li> <li>El método cambia el estado de <code>PENDING_APPROVAL</code> a <code>REJECTED</code></li> <li>El método guarda <code>rejectedById</code>, <code>rejectionReason</code>, y <code>rejectedAt</code></li> <li>El método lanza error si el estado no es <code>PENDING_APPROVAL</code></li> <li>El método valida que <code>reason</code> no esté vacío y tenga máximo 500 caracteres</li> <li>Tests unitarios cubren: rechazo exitoso, error por estado incorrecto, error por PreInvoice no encontrado, error por reason vacío</li> <li>Coverage del método ≥90%</li> <li>LoggerService se usa para logging (no console.*)</li> </ul><hr><p>[S7-B1-04] PreInvoiceConversionService Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-02</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio PreInvoiceConversionService debe crearse en <code>src/lib/services/pre-invoice-conversion.service.ts</code>. El modelo PreInvoice existe con los campos necesarios (subtotal, taxRate, taxAmount, total, items). El modelo Invoice existe con campos similares. El servicio InvoiceService existe en <code>src/lib/services/invoices.service.ts</code>. Para Sprint 7 necesitamos implementar el método <code>convertToInvoice()</code> que convierta un PreInvoice aprobado en una Invoice real usando una transacción atómica.</p><h3>TAREA</h3><p>Crea el servicio PreInvoiceConversionService con el método <code>convertToInvoice(preInvoiceId: string): Promise&lt;Invoice&gt;</code>. El método debe:</p><ol> <li>Validar que el PreInvoice existe</li> <li>Validar que el estado es <code>APPROVED</code> (no <code>PENDING_APPROVAL</code> ni <code>REJECTED</code>)</li> <li>Validar que no esté ya convertido (preInvoiceId en Invoice debe ser null)</li> <li>Crear una Invoice con los mismos datos que el PreInvoice (subtotal, taxRate, taxAmount, total, customerId)</li> <li>Crear InvoiceItems copiando los PreInvoiceItems (productId, quantity, unitPrice, totalPrice)</li> <li>Asociar la Invoice con el PreInvoice (preInvoiceId en Invoice)</li> <li>Cambiar el estado del PreInvoice a <code>CONVERTED</code></li> <li>Usar una transacción atómica de Prisma (todo o nada)</li> <li>Usar LoggerService para logging de la conversión</li> <li>Lanzar error si el PreInvoice no está en estado <code>APPROVED</code></li> <li>Lanzar error si el PreInvoice ya está convertido</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/lib/services/pre-invoice-conversion.service.ts CREAR: src/lib/services/pre-invoice-conversion/<strong>tests</strong>/convert.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">preInvoiceId: string // UUID del PreInvoice aprobado
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">Promise&lt;Invoice&gt; // Invoice creada con los datos del PreInvoice
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno (usar modelos existentes)</p><h3>RESTRICCIONES</h3><ul> <li>NO convertir PreInvoices que no estén en estado <code>APPROVED</code></li> <li>NO convertir PreInvoices ya convertidos</li> <li>NO usar Redis o BullMQ (conversión síncrona)</li> <li>NO llamar a otros servicios desde PreInvoiceConversionService (solo Prisma)</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Usar Prisma.Decimal para montos monetarios</li> <li>La operación debe ser atómica (transacción de Prisma con $transaction)</li> <li>Timeout máximo de 30s para la conversión</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El servicio PreInvoiceConversionService existe</li> <li>El método <code>convertToInvoice()</code> existe</li> <li>El método valida que el PreInvoice esté en estado <code>APPROVED</code></li> <li>El método crea una Invoice con los datos del PreInvoice</li> <li>El método crea InvoiceItems copiando los PreInvoiceItems</li> <li>El método asocia la Invoice con el PreInvoice</li> <li>El método cambia el estado del PreInvoice a <code>CONVERTED</code></li> <li>La operación es atómica (si falla, no se crea nada)</li> <li>Tests unitarios cubren: conversión exitosa, error por estado incorrecto, error por ya convertido, error por PreInvoice no encontrado, rollback en caso de error</li> <li>Coverage del servicio ≥95%</li> <li>LoggerService se usa para logging (no console.*)</li> <li>La conversión completa en &lt;2s (95th percentile)</li> </ul><hr><p>[S7-B1-05] PreInvoice conversion tests Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-04</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio PreInvoiceConversionService fue creado en S7-B1-04 con el método <code>convertToInvoice()</code>. Los tests unitarios básicos fueron creados en <code>src/lib/services/pre-invoice-conversion/__tests__/convert.test.ts</code>. Para Sprint 7 necesitamos agregar tests de integración que validen el flujo completo de conversión con base de datos real.</p><h3>TAREA</h3><p>Crea tests de integración para PreInvoiceConversionService en <code>src/lib/services/pre-invoice-conversion/__tests__/convert.integration.test.ts</code>. Los tests deben:</p><ol> <li>Usar la base de datos de tests (Docker puerto 5434)</li> <li>Crear un PreInvoice con PreInvoiceItems en estado <code>APPROVED</code></li> <li>Ejecutar <code>convertToInvoice()</code></li> <li>Validar que la Invoice se crea correctamente</li> <li>Validar que los InvoiceItems se crean correctamente</li> <li>Validar que el PreInvoice cambia a estado <code>CONVERTED</code></li> <li>Validar que la Invoice está asociada con el PreInvoice</li> <li>Probar rollback: crear un escenario donde falle la conversión y validar que no se crea nada</li> <li>Probar error por estado incorrecto (PENDING_APPROVAL, REJECTED)</li> <li>Probar error por PreInvoice ya convertido</li> <li>Limpiar la base de datos después de cada test</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/lib/services/pre-invoice-conversion/<strong>tests</strong>/convert.integration.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong> Ninguno (tests)</p><p><strong>Output:</strong> Tests de integración pasando</p><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO usar mocks para Prisma Client (usar DB real)</li> <li>NO crear datos que no se limpien después de cada test</li> <li>NO usar Redis o BullMQ</li> <li>Los tests deben ser independientes (no depender del orden)</li> <li>Usar Jest para testing</li> <li>Usar la configuración de tests de integración existente</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El archivo <code>convert.integration.test.ts</code> existe</li> <li>Todos los tests de integración pasan</li> <li>Los tests cubren: conversión exitosa, rollback, error por estado incorrecto, error por ya convertido</li> <li>La base de datos se limpia después de cada test</li> <li>Los tests son independientes</li> <li>Coverage de PreInvoiceConversionService ≥95%</li> <li>Los tests se ejecutan en &lt;30s</li> </ul><hr><p>[S7-B1-06] API endpoint - approve Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-02</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El método <code>approve()</code> fue implementado en PreInvoiceService en S7-B1-02. Los endpoints de API están en <code>src/app/api/v1/</code>. Para Sprint 7 necesitamos crear el endpoint <code>POST /api/v1/pre-invoices/:id/approve</code> que permita aprobar un PreInvoice. El endpoint debe validar la request con Zod, llamar a PreInvoiceService, y retornar el PreInvoice actualizado.</p><h3>TAREA</h3><p>Crea el endpoint <code>POST /api/v1/pre-invoices/:id/approve</code> en <code>src/app/api/v1/pre-invoices/[id]/approve/route.ts</code>. El endpoint debe:</p><ol> <li>Extraer el <code>id</code> de la URL</li> <li>Validar que el <code>id</code> es un UUID válido</li> <li>Extraer el <code>userId</code> del header <code>x-user-id</code> o del body</li> <li>Validar la request con Zod (userId es requerido)</li> <li>Llamar a <code>PreInvoiceService.approve(id, userId)</code></li> <li>Retornar 200 con el PreInvoice actualizado en JSON</li> <li>Retornar 400 si la validación falla</li> <li>Retornar 404 si el PreInvoice no existe</li> <li>Retornar 400 si el estado no es <code>PENDING_APPROVAL</code></li> <li>Usar LoggerService para logging de la request</li> <li>Usar correlation ID (x-correlation-id header)</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/app/api/v1/pre-invoices/[id]/approve/route.ts CREAR: src/app/api/v1/pre-invoices/[id]/approve/<strong>tests</strong>/route.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">// URL parameter
id: string // UUID del PreInvoice

// Header
x-user-id: string // userId del usuario que aprueba
x-correlation-id: string // correlation ID (opcional)

// Body (opcional)
{
  userId?: string // userId del usuario que aprueba (alternativa al header)
}
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">// 200 OK
{
  id: string,
  customerId: string,
  status: "APPROVED",
  approvedById: string,
  approvedAt: string,
  // ... otros campos del PreInvoice
}

// 400 Bad Request
{
  error: string,
  message: string
}

// 404 Not Found
{
  error: string,
  message: string
}
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO aprobar PreInvoices que no estén en estado <code>PENDING_APPROVAL</code></li> <li>NO permitir aprobar sin userId válido</li> <li>NO usar Redis o BullMQ</li> <li>NO llamar a otros servicios desde el endpoint (solo PreInvoiceService)</li> <li>Usar Zod para validación de la request</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Usar correlation ID (x-correlation-id header)</li> <li>El endpoint debe ser idempotente (aprobar dos veces el mismo PreInvoice retorna error la segunda vez)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El endpoint <code>POST /api/v1/pre-invoices/:id/approve</code> existe</li> <li>El endpoint valida el <code>id</code> como UUID válido</li> <li>El endpoint valida el <code>userId</code> (header o body)</li> <li>El endpoint llama a <code>PreInvoiceService.approve()</code></li> <li>El endpoint retorna 200 con el PreInvoice actualizado</li> <li>El endpoint retorna 400 si la validación falla</li> <li>El endpoint retorna 404 si el PreInvoice no existe</li> <li>El endpoint retorna 400 si el estado no es <code>PENDING_APPROVAL</code></li> <li>Tests unitarios cubren: aprobación exitosa, error por UUID inválido, error por userId faltante, error por PreInvoice no encontrado, error por estado incorrecto</li> <li>LoggerService se usa para logging (no console.*)</li> <li>Correlation ID se usa en logging</li> </ul><hr><p>[S7-B1-07] API endpoint - reject Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-03</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El método <code>reject()</code> fue implementado en PreInvoiceService en S7-B1-03. Los endpoints de API están en <code>src/app/api/v1/</code>. Para Sprint 7 necesitamos crear el endpoint <code>POST /api/v1/pre-invoices/:id/reject</code> que permita rechazar un PreInvoice con un motivo. El endpoint debe validar la request con Zod, llamar a PreInvoiceService, y retornar el PreInvoice actualizado.</p><h3>TAREA</h3><p>Crea el endpoint <code>POST /api/v1/pre-invoices/:id/reject</code> en <code>src/app/api/v1/pre-invoices/[id]/reject/route.ts</code>. El endpoint debe:</p><ol> <li>Extraer el <code>id</code> de la URL</li> <li>Validar que el <code>id</code> es un UUID válido</li> <li>Extraer el <code>userId</code> del header <code>x-user-id</code> o del body</li> <li>Extraer el <code>reason</code> del body</li> <li>Validar la request con Zod (userId y reason son requeridos, reason 1-500 caracteres)</li> <li>Llamar a <code>PreInvoiceService.reject(id, userId, reason)</code></li> <li>Retornar 200 con el PreInvoice actualizado en JSON</li> <li>Retornar 400 si la validación falla</li> <li>Retornar 404 si el PreInvoice no existe</li> <li>Retornar 400 si el estado no es <code>PENDING_APPROVAL</code></li> <li>Usar LoggerService para logging de la request</li> <li>Usar correlation ID (x-correlation-id header)</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/app/api/v1/pre-invoices/[id]/reject/route.ts CREAR: src/app/api/v1/pre-invoices/[id]/reject/<strong>tests</strong>/route.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">// URL parameter
id: string // UUID del PreInvoice

// Header
x-user-id: string // userId del usuario que rechaza
x-correlation-id: string // correlation ID (opcional)

// Body
{
  userId?: string, // userId del usuario que rechaza (alternativa al header)
  reason: string // Motivo del rechazo (1-500 caracteres)
}
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">// 200 OK
{
  id: string,
  customerId: string,
  status: "REJECTED",
  rejectedById: string,
  rejectionReason: string,
  rejectedAt: string,
  // ... otros campos del PreInvoice
}

// 400 Bad Request
{
  error: string,
  message: string
}

// 404 Not Found
{
  error: string,
  message: string
}
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO rechazar PreInvoices que no estén en estado <code>PENDING_APPROVAL</code></li> <li>NO permitir rechazar sin userId válido o reason vacío</li> <li>NO usar Redis o BullMQ</li> <li>NO llamar a otros servicios desde el endpoint (solo PreInvoiceService)</li> <li>Usar Zod para validación de la request</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Usar correlation ID (x-correlation-id header)</li> <li>Validar que <code>reason</code> tenga entre 1 y 500 caracteres</li> <li>El endpoint debe ser idempotente (rechazar dos veces el mismo PreInvoice retorna error la segunda vez)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El endpoint <code>POST /api/v1/pre-invoices/:id/reject</code> existe</li> <li>El endpoint valida el <code>id</code> como UUID válido</li> <li>El endpoint valida el <code>userId</code> (header o body)</li> <li>El endpoint valida el <code>reason</code> (1-500 caracteres)</li> <li>El endpoint llama a <code>PreInvoiceService.reject()</code></li> <li>El endpoint retorna 200 con el PreInvoice actualizado</li> <li>El endpoint retorna 400 si la validación falla</li> <li>El endpoint retorna 404 si el PreInvoice no existe</li> <li>El endpoint retorna 400 si el estado no es <code>PENDING_APPROVAL</code></li> <li>Tests unitarios cubren: rechazo exitoso, error por UUID inválido, error por userId faltante, error por reason vacío, error por reason muy largo, error por PreInvoice no encontrado, error por estado incorrecto</li> <li>LoggerService se usa para logging (no console.*)</li> <li>Correlation ID se usa en logging</li> </ul><hr><p>[S7-B1-08] API endpoint - convert Bloque: BLOQUE 1 - PREINVOICE CONVERSION | Dependencias: S7-B1-04</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio PreInvoiceConversionService fue creado en S7-B1-04 con el método <code>convertToInvoice()</code>. Los endpoints de API están en <code>src/app/api/v1/</code>. Para Sprint 7 necesitamos crear el endpoint <code>POST /api/v1/pre-invoices/:id/convert</code> que permita convertir un PreInvoice aprobado en una Invoice. El endpoint debe validar la request con Zod, llamar a PreInvoiceConversionService, y retornar la Invoice creada.</p><h3>TAREA</h3><p>Crea el endpoint <code>POST /api/v1/pre-invoices/:id/convert</code> en <code>src/app/api/v1/pre-invoices/[id]/convert/route.ts</code>. El endpoint debe:</p><ol> <li>Extraer el <code>id</code> de la URL</li> <li>Validar que el <code>id</code> es un UUID válido</li> <li>Llamar a <code>PreInvoiceConversionService.convertToInvoice(id)</code></li> <li>Retornar 201 con la Invoice creada en JSON</li> <li>Retornar 400 si la validación falla</li> <li>Retornar 404 si el PreInvoice no existe</li> <li>Retornar 400 si el estado no es <code>APPROVED</code></li> <li>Retornar 400 si el PreInvoice ya está convertido</li> <li>Usar LoggerService para logging de la request</li> <li>Usar correlation ID (x-correlation-id header)</li> <li>Implementar timeout de 30s para la conversión</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/app/api/v1/pre-invoices/[id]/convert/route.ts CREAR: src/app/api/v1/pre-invoices/[id]/convert/<strong>tests</strong>/route.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">// URL parameter
id: string // UUID del PreInvoice

// Header
x-correlation-id: string // correlation ID (opcional)
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">// 201 Created
{
  id: string,
  number: string,
  customerId: string,
  status: "DRAFT",
  subtotal: number,
  taxRate: number,
  taxAmount: number,
  total: number,
  preInvoiceId: string,
  // ... otros campos de la Invoice
}

// 400 Bad Request
{
  error: string,
  message: string
}

// 404 Not Found
{
  error: string,
  message: string
}
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO convertir PreInvoices que no estén en estado <code>APPROVED</code></li> <li>NO convertir PreInvoices ya convertidos</li> <li>NO usar Redis o BullMQ (conversión síncrona)</li> <li>NO llamar a otros servicios desde el endpoint (solo PreInvoiceConversionService)</li> <li>Usar Zod para validación de la request</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Usar correlation ID (x-correlation-id header)</li> <li>Implementar timeout de 30s para la conversión</li> <li>El endpoint debe ser idempotente (convertir dos veces el mismo PreInvoice retorna error la segunda vez)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>El endpoint <code>POST /api/v1/pre-invoices/:id/convert</code> existe</li> <li>El endpoint valida el <code>id</code> como UUID válido</li> <li>El endpoint llama a <code>PreInvoiceConversionService.convertToInvoice()</code></li> <li>El endpoint retorna 201 con la Invoice creada</li> <li>El endpoint retorna 400 si la validación falla</li> <li>El endpoint retorna 404 si el PreInvoice no existe</li> <li>El endpoint retorna 400 si el estado no es <code>APPROVED</code></li> <li>El endpoint retorna 400 si el PreInvoice ya está convertido</li> <li>Tests unitarios cubren: conversión exitosa, error por UUID inválido, error por PreInvoice no encontrado, error por estado incorrecto, error por ya convertido, timeout</li> <li>LoggerService se usa para logging (no console.*)</li> <li>Correlation ID se usa en logging</li> <li>La conversión completa en &lt;2s (95th percentile)</li> </ul><hr><p>[S7-B2-01] NotificationService - PreInvoice events Bloque: BLOQUE 2 - NOTIFICACIONES | Dependencias: S7-B1-02, S7-B1-03</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio NotificationService existe en <code>src/lib/notifications/service.ts</code>. Los servicios de email (Resend) y chat (Twilio, Telegram) existen en <code>src/lib/services/email.service.ts</code> y <code>src/lib/services/bot/whatsapp.service.ts</code>, <code>src/lib/services/bot/telegram.service.ts</code>. Para Sprint 7 necesitamos implementar notificaciones para eventos de PreInvoice: cuando un PreInvoice requiere aprobación (PENDING_APPROVAL), cuando es aprobado (APPROVED), y cuando es rechazado (REJECTED).</p><h3>TAREA</h3><p>Implementa métodos en NotificationService para enviar notificaciones de eventos de PreInvoice. Los métodos deben:</p><ol> <li><code>notifyPreInvoicePendingApproval(preInvoiceId: string)</code> - Enviar notificación cuando PreInvoice está en estado PENDING_APPROVAL</li> <li><code>notifyPreInvoiceApproved(preInvoiceId: string)</code> - Enviar notificación cuando PreInvoice es aprobado</li> <li><code>notifyPreInvoiceRejected(preInvoiceId: string, reason: string)</code> - Enviar notificación cuando PreInvoice es rechazado</li> <li>Cada método debe enviar notificación por email y por chat (WhatsApp/Telegram)</li> <li>Usar EmailService para email</li> <li>Usar WhatsAppService y TelegramService para chat</li> <li>Usar LoggerService para logging</li> <li>Validar que el PreInvoice existe antes de enviar notificaciones</li> <li>Incluir información relevante en la notificación (id, customerId, total, status, reason si aplica)</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>MODIFICAR: src/lib/notifications/service.ts CREAR: src/lib/notifications/<strong>tests</strong>/pre-invoice-events.test.ts</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">// notifyPreInvoicePendingApproval
preInvoiceId: string // UUID del PreInvoice

// notifyPreInvoiceApproved
preInvoiceId: string // UUID del PreInvoice

// notifyPreInvoiceRejected
preInvoiceId: string // UUID del PreInvoice
reason: string // Motivo del rechazo
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">Promise&lt;void&gt; // Notificaciones enviadas
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO enviar notificaciones si el PreInvoice no existe</li> <li>NO usar Redis o BullMQ (envío síncrono)</li> <li>NO crear nuevos servicios de notificación (usar existentes)</li> <li>Usar EmailService para email (Resend)</li> <li>Usar WhatsAppService y TelegramService para chat</li> <li>Usar LoggerService para logging (no console.*)</li> <li>Las notificaciones deben ser asíncronas (Promise.all para email + chat)</li> <li>Si una notificación falla, las otras deben intentar enviarse (no abortar todo)</li> </ul><h3>CRITERIO DE DONE</h3><ul> <li>Los 3 métodos existen en NotificationService</li> <li>Los métodos envían notificaciones por email y chat</li> <li>Los métodos validan que el PreInvoice existe</li> <li>Los métodos incluyen información relevante en la notificación</li> <li>Tests unitarios cubren: notificación exitosa, error por PreInvoice no encontrado, error por email falla, error por chat falla</li> <li>Coverage de los métodos ≥90%</li> <li>LoggerService se usa para logging (no console.*)</li> </ul><hr><p>[S7-B2-02] Email template - approval request Bloque: BLOQUE 2 - NOTIFICACIONES | Dependencias: S7-B2-01</p><h3>CONTEXTO DEL REPOSITORIO</h3><p>El servicio EmailService existe en <code>src/lib/services/email.service.ts</code> y usa Resend para enviar emails. Para Sprint 7 necesitamos crear un template HTML para el email de solicitud de aprobación de PreInvoice. El template debe ser profesional, incluir la información del PreInvoice, y tener botones para aprobar/rechazar.</p><h3>TAREA</h3><p>Crea un template HTML para el email de solicitud de aprobación de PreInvoice en <code>src/lib/emails/templates/pre-invoice-approval-request.html</code>. El template debe:</p><ol> <li>Ser responsive (funcionar en móvil y desktop)</li> <li>Incluir el logo de Hago Produce (usar placeholder si no existe)</li> <li>Incluir información del PreInvoice: id, customerId, total, fecha de creación</li> <li>Incluir un botón para aprobar (link a <code>/pre-invoices/:id/approve</code>)</li> <li>Incluir un botón para rechazar (link a <code>/pre-invoices/:id/reject</code>)</li> <li>Tener un diseño profesional con colores corporativos</li> <li>Ser compatible con Resend</li> <li>Usar variables de template (ej: {{preInvoiceId}}, {{customerId}}, {{total}})</li> </ol><h3>ARCHIVOS A CREAR / MODIFICAR</h3><p>CREAR: src/lib/emails/templates/pre-invoice-approval-request.html MODIFICAR: src/lib/services/email.service.ts (agregar método para renderizar template)</p><h3>CONTRATO TÉCNICO</h3><p><strong>Input:</strong></p><pre><code class="language-typescript">{
  preInvoiceId: string,
  customerId: string,
  customerName: string,
  total: number,
  currency: string,
  createdAt: string,
  approveUrl: string,
  rejectUrl: string
}
</code></pre><p><strong>Output:</strong></p><pre><code class="language-typescript">string // HTML del email renderizado
</code></pre><p><strong>Variables de entorno nuevas:</strong> Ninguna</p><p><strong>Schema Prisma:</strong> Ninguno</p><h3>RESTRICCIONES</h3><ul> <li>NO usar frameworks de templates complejos (Handlebars, EJS) - usar interpolación simple</li> <li>NO incluir estilos inline (usar </li></ul>